---
phase: 04-ci-cd-auto-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_handlers.py
  - requirements-dev.txt
autonomous: true
requirements: [QA-01, QA-02]
user_setup: []

must_haves:
  truths:
    - "pytest tests/test_handlers.py exits 0"
    - "handle_text replies with the LLM output on the happy path"
    - "handle_text sends a friendly error message when the complete() call raises"
    - "handle_text rejects a chat_id not in a non-empty allowlist and never calls complete()"
    - "handle_text is a no-op (no crash) when update.message is None"
    - "/start and /help reply with START_TEXT and HELP_TEXT"
    - "requirements-dev.txt pins ruff and mypy so CI installs them from one source of truth"
  artifacts:
    - path: "tests/test_handlers.py"
      provides: "Unit tests for start, help_cmd, handle_text (happy/error/allowlist/no-op)"
      contains: "def test_handle_text"
      min_lines: 50
    - path: "requirements-dev.txt"
      provides: "Pinned dev tooling (pytest, ruff, mypy)"
      contains: "ruff"
  key_links:
    - from: "tests/test_handlers.py"
      to: "bot.handlers.handle_text"
      via: "import + asyncio.run"
      pattern: "from bot.handlers import"
    - from: "tests/test_handlers.py"
      to: "context.bot_data['complete']"
      via: "AsyncMock"
      pattern: "AsyncMock"
---

<objective>
Close the QA-01 test gap and the dev-tooling parity gap (QA-02). Create `tests/test_handlers.py`
covering the three PTB handlers (`start`, `help_cmd`, `handle_text`) using the repo's existing
`asyncio.run()` + `AsyncMock`/`MagicMock` idiom, and pin `ruff` + `mypy` into `requirements-dev.txt`
so CI installs all dev tooling from one source of truth.

Purpose: QA-01 requires `pytest` to cover core message handling (the OpenAI call path is already
covered by `tests/test_openai_client.py`; the gap is the handlers). QA-02 requires CI to run from
pinned tooling rather than ad-hoc `pip install ruff mypy` on the command line.
Output: `tests/test_handlers.py` (new), `requirements-dev.txt` (updated with pinned ruff + mypy).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ci-cd-auto-deploy/04-RESEARCH.md
@bot/handlers.py
@bot/prompts.py
@bot/main.py
@tests/test_openai_client.py
@tests/conftest.py
@requirements-dev.txt
</context>

<artifacts_this_phase_produces>
This plan creates/changes the following symbols and files:
- `tests/test_handlers.py` — new test module. Test functions (names are load-bearing; gates assert them):
  - `test_handle_text_replies_with_llm_output`
  - `test_handle_text_friendly_error_on_llm_failure`
  - `test_handle_text_rejects_unauthorized_chat`
  - `test_handle_text_no_message_is_noop`
  - `test_start_sends_welcome`
  - `test_help_sends_usage`
  - plus local helpers `_make_update(...)` / `_make_context(...)` (mirroring `test_openai_client.py` `_make_mock_response`)
- `requirements-dev.txt` — adds pinned `ruff==0.12.0` and `mypy==1.17.1` (the installed/CI versions per RESEARCH).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/test_handlers.py covering start, help_cmd, and handle_text</name>
  <files>tests/test_handlers.py</files>
  <read_first>
    - bot/handlers.py (the code under test — note: `context.bot_data["complete"]` is a SINGLE-ARG callable invoked as `complete(update.message.text)`, NOT `(model, key, text)`; `handle_text` early-returns when `update.message is None OR update.effective_chat is None`; the unauthorized branch fires only when `allowed_chat_ids` is truthy AND chat_id not in it)
    - bot/prompts.py (exact constants START_TEXT and HELP_TEXT to assert against)
    - tests/test_openai_client.py (the idiom to mirror: top-level `def test_*`, `asyncio.run(coro)`, `AsyncMock`/`MagicMock`, no `@pytest.mark.asyncio`, no pytest config)
    - bot/main.py (how `complete` and `allowed_chat_ids` are wired into `bot_data` — confirms the single-arg lambda and the frozenset allowlist)
    - .planning/phases/04-ci-cd-auto-deploy/04-RESEARCH.md (Code Examples section — grounded handler test patterns)
  </read_first>
  <behavior>
    - test_handle_text_replies_with_llm_output: given message text "hello" and an allowed (or empty-allowlist) chat, `handle_text` awaits `context.bot_data["complete"]` exactly once with "hello" and awaits `update.message.reply_text` exactly once with the returned reply string.
    - test_handle_text_friendly_error_on_llm_failure: when `context.bot_data["complete"]` raises (AsyncMock side_effect=RuntimeError), `handle_text` does not propagate, and `reply_text` is awaited once with a message containing the substring "went wrong".
    - test_handle_text_rejects_unauthorized_chat: with `allowed_chat_ids=frozenset({123})` and `update.effective_chat.id=999`, `reply_text` is awaited once with a message containing "not authorized" and `complete` is never awaited.
    - test_handle_text_no_message_is_noop: with `update.message = None`, `handle_text` returns without raising and without touching `complete`.
    - test_start_sends_welcome: `start` awaits `reply_text` once with exactly `START_TEXT`.
    - test_help_sends_usage: `help_cmd` awaits `reply_text` once with exactly `HELP_TEXT`.
  </behavior>
  <action>
    Create `tests/test_handlers.py` in the same style as `tests/test_openai_client.py`: module-level
    `def test_*` functions driven by `asyncio.run(...)`, no `@pytest.mark.asyncio`, no new pytest config.
    Import the handlers via `from bot.handlers import start, help_cmd, handle_text` and the constants via
    `from bot.prompts import START_TEXT, HELP_TEXT`.
    Add two local helpers (mirroring `_make_mock_response` in the OpenAI test):
    `_make_update(text="hello", chat_id=123)` returns a `MagicMock` where `update.message.text=text`,
    `update.message.reply_text=AsyncMock()`, and `update.effective_chat.id=chat_id`;
    `_make_context(reply="hi back", allowed=frozenset())` returns a `MagicMock` whose
    `bot_data` is a real dict `{"complete": AsyncMock(return_value=reply), "allowed_chat_ids": allowed}`
    (use a real dict, not a MagicMock attribute, because the handler does `context.bot_data["complete"]`
    and `context.bot_data.get("allowed_chat_ids", frozenset())`).
    Implement exactly the six test functions named in <behavior>. CRITICAL: mock `complete` as a
    single-argument AsyncMock and assert it is awaited with the text only (e.g.
    `assert_awaited_once_with("hello")`) — the handler passes one positional arg. For the error-message
    assertions, read the literal strings from `bot/handlers.py` ("Sorry, something went wrong..." contains
    "went wrong"; "Sorry, you are not authorized to use this bot." contains "not authorized") and assert on
    those substrings via `update.message.reply_text.call_args.args[0]`. For start/help, assert
    `reply_text.assert_awaited_once_with(START_TEXT)` / `(HELP_TEXT)` respectively. Do not add fixtures to
    conftest.py — conftest fixtures are config-only and not needed here.
  </action>
  <verify>
    <automated>pytest tests/test_handlers.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_handlers.py contains `def test_handle_text_replies_with_llm_output`
    - tests/test_handlers.py contains `def test_handle_text_friendly_error_on_llm_failure`
    - tests/test_handlers.py contains `def test_handle_text_rejects_unauthorized_chat`
    - tests/test_handlers.py contains `def test_handle_text_no_message_is_noop`
    - tests/test_handlers.py contains `def test_start_sends_welcome`
    - tests/test_handlers.py contains `def test_help_sends_usage`
    - tests/test_handlers.py contains `from bot.handlers import` and `from bot.prompts import`
    - `pytest tests/test_handlers.py -q` exits 0 with 6 passed
    - `ruff check tests/test_handlers.py` exits 0 and `ruff format --check tests/test_handlers.py` exits 0
  </acceptance_criteria>
  <done>All six handler tests pass under `pytest tests/test_handlers.py -q`, lint and format checks pass, and the file mirrors the existing `asyncio.run()` test idiom (no new pytest config, no conftest changes).</done>
</task>

<task type="auto">
  <name>Task 2: Pin ruff and mypy in requirements-dev.txt</name>
  <files>requirements-dev.txt</files>
  <read_first>
    - requirements-dev.txt (current contents: `-r requirements.txt` then `pytest` — ruff and mypy are missing)
    - .github/workflows/ci.yml (shows CI currently installs them ad-hoc: `pip install -r requirements-dev.txt ruff mypy` — the parity gap)
    - .planning/phases/04-ci-cd-auto-deploy/04-RESEARCH.md (Standard Stack: pin `ruff==0.12.0`, `mypy==1.17.1` — the installed/CI versions; pytest-asyncio is NOT needed since tests use `asyncio.run()`)
  </read_first>
  <action>
    Add two pinned lines to `requirements-dev.txt` after the existing `pytest` line: `ruff==0.12.0` and
    `mypy==1.17.1` (the versions RESEARCH confirms are installed and that CI will use). Keep the existing
    `-r requirements.txt` and `pytest` lines. Do NOT add `pytest-asyncio` — the test suite uses the
    `asyncio.run()` idiom and needs no async plugin or config. The goal is that
    `pip install -r requirements-dev.txt` alone installs every tool CI runs (pytest, ruff, mypy), so the
    Wave 2 workflow can drop the ad-hoc `ruff mypy` arguments.
  </action>
  <verify>
    <automated>grep -v '^#' requirements-dev.txt | grep -c -E '^(ruff|mypy)==' | grep -qx 2 && pip install -r requirements-dev.txt --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - requirements-dev.txt contains a line matching `ruff==0.12.0`
    - requirements-dev.txt contains a line matching `mypy==1.17.1`
    - requirements-dev.txt still contains `-r requirements.txt` and `pytest`
    - requirements-dev.txt does NOT contain `pytest-asyncio`
    - `pip install -r requirements-dev.txt --dry-run` resolves without error
  </acceptance_criteria>
  <done>requirements-dev.txt pins ruff==0.12.0 and mypy==1.17.1 alongside pytest, with no pytest-asyncio, and resolves cleanly — CI can now install all dev tooling from this one file.</done>
</task>

</tasks>

<verification>
- `pytest -q` (full suite) exits 0 — the new handler tests pass alongside the existing config and OpenAI-client tests.
- `ruff check . && ruff format --check .` exits 0.
- `mypy bot/ --ignore-missing-imports` exits 0 (test files are intentionally out of mypy scope per RESEARCH Pitfall 5).
- `pip install -r requirements-dev.txt --dry-run` installs pytest, ruff, and mypy without the ad-hoc `ruff mypy` arguments.
</verification>

<success_criteria>
- QA-01: `tests/test_handlers.py` covers core message handling (handle_text happy/error/allowlist/no-op) and the command handlers (start, help); combined with the existing `tests/test_openai_client.py`, the OpenAI call path and message handling are both under automated test.
- QA-02 (parity half): `requirements-dev.txt` is the single source of truth for dev tooling, pinning ruff and mypy to the CI versions.
</success_criteria>

<output>
Create `.planning/phases/04-ci-cd-auto-deploy/04-01-SUMMARY.md` when done.
</output>
