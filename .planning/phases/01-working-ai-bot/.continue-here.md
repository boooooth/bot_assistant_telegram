---
context: phase
phase: 01-working-ai-bot
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-06-12T01:56:38.642Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: direct-openai-only — No provider abstraction, no `LLMProvider` Protocol, no factory, no `import anthropic`. Call `AsyncOpenAI.chat.completions.create` directly. CLAUDE.md was cleaned to remove stale adapter references (commit f004be2); do NOT re-introduce them.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Provider abstraction | Any `LLMProvider` Protocol, factory function, or `anthropic` import — this decision was reversed and CLAUDE.md was fixed | blocking | Only `bot/openai_client.py` may import `openai`; grep for `anthropic` / `factory` / `Provider` before committing |
| concurrent_updates in Phase 1 | Adding `concurrent_updates=True` to ApplicationBuilder — this belongs to Phase 2 reliability hardening | advisory | Check 01-02-PLAN.md Task 3 notes; omit in main.py |
| Logging message bodies | Logging full user text or AI reply content at INFO — privacy/security concern (T-01-05) | advisory | Log chat id + status only, never message content or secrets |

<current_state>
Wave 1 (01-01-PLAN.md) is fully complete and committed (3 commits):
- c8ffea9: scaffold bot package, pinned deps, secrets hygiene
- 44b5651: failing Wave 0 tests for fail-fast config loader (TDD red phase)
- 81d5275: implement fail-fast config loader (green phase)

Wave 2 (01-02-PLAN.md) has NOT started. No Wave 2 files exist yet (no bot/prompts.py, bot/openai_client.py, bot/handlers.py, bot/main.py, bot/__main__.py, tests/test_openai_client.py, README.md).

Uncommitted changes: .planning/STATE.md and .planning/config.json are modified but not staged.

MISSING: 01-01-SUMMARY.md was not created after Wave 1 completion. The 01-02-PLAN.md context block references it — create it before executing Wave 2.
</current_state>

<completed_work>
**Wave 1 — 01-01-PLAN.md (DONE, committed)**

- Task 1: Scaffold project + secrets-hygiene files + dependencies
  - bot/__init__.py (empty package marker)
  - requirements.txt (python-telegram-bot==22.7, openai==2.41.1, python-dotenv>=1.0,<2) — NO anthropic
  - requirements-dev.txt (-r requirements.txt + pytest)
  - .gitignore (.env + Python cruft gitignored from commit #1)
  - .env.example (TELEGRAM_BOT_TOKEN=, OPENAI_API_KEY=, OPENAI_MODEL=gpt-4o-mini)
- Task 2: Fail-fast config loader + Wave 0 config tests
  - bot/config.py: ConfigError, frozen Settings dataclass, load_settings() — single os.environ reader
  - tests/__init__.py, tests/conftest.py, tests/test_config.py — pytest green
  - pytest tests/test_config.py -x passes (fail-fast + gpt-4o-mini default verified)
</completed_work>

<remaining_work>
**Wave 2 — 01-02-PLAN.md (NOT STARTED)**

Pre-requisite: Create 01-01-SUMMARY.md first (Wave 2 context block reads it).

- Task 1: bot/prompts.py — SYSTEM_PROMPT (D-01/D-04/D-05), START_TEXT (D-02), HELP_TEXT (D-03)
- Task 2 (TDD): bot/openai_client.py + tests/test_openai_client.py — direct AsyncOpenAI one-shot call, MSG-02 messages=[system,user] test
- Task 3: bot/handlers.py, bot/main.py, bot/__main__.py, README.md — full composition root wired to `python -m bot`
- Task 4 (BLOCKING HUMAN GATE): Live smoke test in Telegram — requires TELEGRAM_BOT_TOKEN + OPENAI_API_KEY in .env
</remaining_work>

<decisions_made>
- **Direct OpenAI only** — no abstraction, no factory, no anthropic; bot/openai_client.py is the only openai importer
- **gpt-4o-mini default** — via OPENAI_MODEL env var (LLM-01)
- **DO NOT add concurrent_updates** in Phase 1 — deferred to Phase 2
- **python-dotenv loads .env in main()** before load_settings() — local dev reads .env; Docker uses real env vars
- **No max_tokens cap** in v1 (D-04, deferred); soft-nudge conciseness via SYSTEM_PROMPT only
- **Reply in user's language** (D-05) via SYSTEM_PROMPT instruction
- **Log chat id + status only** — never message body/reply/secrets (T-01-05)
- **Separate dev BotFather token** recommended for local testing (avoids 409 conflicts with production — REL-03)
</decisions_made>

<blockers>
- **01-01-SUMMARY.md missing**: Wave 2 plan reads it as context. Create before executing 01-02.
- **Credentials needed for Task 4**: TELEGRAM_BOT_TOKEN (BotFather /newbot → HTTP API token) and OPENAI_API_KEY (OpenAI Dashboard) must be in .env before the live smoke test. Use a dev-only BotFather token, NOT the future production token.
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-working-ai-bot/01-02-PLAN.md` — Wave 2 tasks, must_haves, acceptance criteria, and the human checkpoint instructions
2. `bot/config.py` — Settings / load_settings consumed by main.py
3. `.planning/phases/01-working-ai-bot/01-RESEARCH.md` — Code Examples §1–§5 (main, openai, handlers, prompts, __main__) and Pattern 2 (framework polling loop)
4. `.planning/phases/01-working-ai-bot/01-CONTEXT.md` — Decisions D-01..D-05 (persona/copy/language)
5. `.planning/phases/01-working-ai-bot/01-VALIDATION.md` — Wave 0 MSG-02 test + Manual-Only checkpoints

## Infrastructure State
- Python deps: installed (pytest, python-telegram-bot, openai, python-dotenv)
- pytest tests/test_config.py: GREEN
- .env: does NOT exist yet (user has not yet filled credentials) — needs TELEGRAM_BOT_TOKEN + OPENAI_API_KEY for Wave 2 Task 4
- git: on branch master, 5 commits (c8ffea9 is HEAD of Wave 1)
- Uncommitted: .planning/STATE.md, .planning/config.json (modified but not staged — not critical to Wave 2)

<context>
The project has a clean Wave 1 foundation: config loads, tests pass, deps are pinned. Wave 2 is the interesting part — wire prompts → OpenAI client → handlers → composition root into a real working Telegram bot. The architecture is fully designed and the code examples are in 01-RESEARCH.md. Execution should be straightforward: follow 01-02-PLAN.md task by task, TDD for openai_client.py, then the human smoke test gates the phase.

The user switched model to Sonnet 4.6 before pausing — Wave 2 execution was the next step.
</context>

<next_action>
1. Create `.planning/phases/01-working-ai-bot/01-01-SUMMARY.md` (Wave 1 completion summary — referenced by 01-02-PLAN.md context block).
2. Execute Wave 2 — run `/gsd-execute-plan 1 02` or `/gsd-execute-phase 1` which will pick up Wave 2 (01-02-PLAN.md).
3. Before Task 4 (live smoke test), ensure user has `.env` filled with TELEGRAM_BOT_TOKEN + OPENAI_API_KEY.
</next_action>
