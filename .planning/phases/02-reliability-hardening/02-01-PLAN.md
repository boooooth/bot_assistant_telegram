---
phase: 02-reliability-hardening
plan: 01
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - bot/openai_client.py
autonomous: true
requirements: [REL-01, REL-02, REL-03]
---

<objective>
Harden the working bot so it survives real public traffic: add a 30-second timeout to the LiteLLM call so a slow or hanging OpenAI response fails fast and returns a friendly error to the user instead of waiting forever.

REL-01 (friendly error on failure) and REL-02 (slow calls don't block others) are already satisfied by the existing try/except in handlers.py and PTB's async architecture. REL-03 (single poller) is a deployment concern addressed in Phase 3. The only code gap is the missing timeout on the LiteLLM call.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add timeout to LiteLLM call</name>
  <files>bot/openai_client.py</files>
  <action>
    Add `timeout=30` parameter to `litellm.acompletion()` in `bot/openai_client.py`.
    If OpenAI does not respond within 30 seconds, LiteLLM raises an exception which the
    existing `except Exception` block in `handlers.py` catches and returns a friendly
    error message to the user.
  </action>
  <acceptance_criteria>
    - `bot/openai_client.py` contains `timeout=30` in the `litellm.acompletion()` call
  </acceptance_criteria>
</task>

</tasks>

<output>
Create `.planning/phases/02-reliability-hardening/02-01-SUMMARY.md` when done.
</output>
