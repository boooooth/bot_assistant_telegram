---
phase: 05-ux-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - bot/prompts.py
  - bot/handlers.py
  - bot/main.py
  - tests/test_handlers.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "A user who sends a non-text message (photo, voice, sticker, video, document) gets a helpful 'text only' reply instead of a silent drop"
    - "The non-text guard applies to ALL users, including those not in allowed_chat_ids (D-08)"
    - "A single MessageHandler with ~filters.TEXT & ~filters.COMMAND catches every non-text type with no per-type branching (D-06)"
  artifacts:
    - path: "bot/prompts.py"
      provides: "NON_TEXT_REPLY constant"
      contains: "NON_TEXT_REPLY"
    - path: "bot/handlers.py"
      provides: "handle_non_text guard handler"
      contains: "async def handle_non_text("
    - path: "bot/main.py"
      provides: "registration of handle_non_text with inverted filter"
      contains: "handle_non_text"
    - path: "tests/test_handlers.py"
      provides: "tests for the non-text guard message and None-message no-op"
      contains: "def test_handle_non_text_"
  key_links:
    - from: "bot/main.py"
      to: "bot/handlers.py:handle_non_text"
      via: "MessageHandler(~filters.TEXT & ~filters.COMMAND, handle_non_text)"
      pattern: "~filters.TEXT & ~filters.COMMAND"
    - from: "bot/handlers.py:handle_non_text"
      to: "bot/prompts.py:NON_TEXT_REPLY"
      via: "reply_text(NON_TEXT_REPLY)"
      pattern: "NON_TEXT_REPLY"
---

<objective>
Add a non-text message guard (D-06, D-07, D-08): a single MessageHandler that catches all
non-text, non-command messages (photo, voice, sticker, video, document) and replies with a
helpful "I only understand text" nudge for every user — no more silent drops.

Purpose: Users who send media currently get nothing back. This gives them clear, friendly
feedback so they know the bot only handles text.
Output: New `NON_TEXT_REPLY` constant in `bot/prompts.py`, new `handle_non_text` handler in
`bot/handlers.py`, its registration in `bot/main.py`, and tests in `tests/test_handlers.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-polish/05-CONTEXT.md
@.planning/phases/05-ux-polish/05-PATTERNS.md
@bot/handlers.py
@bot/main.py
@bot/prompts.py
@tests/test_handlers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NON_TEXT_REPLY constant to bot/prompts.py</name>
  <files>bot/prompts.py</files>
  <read_first>
    - bot/prompts.py (current constants and style; note TRUNCATION_NOTE added by plan 05-01 — keep both)
    - .planning/phases/05-ux-polish/05-CONTEXT.md (D-07: helpful-nudge tone; Claude's Discretion: exact wording)
  </read_first>
  <action>
    Add a module-level constant `NON_TEXT_REPLY` to bot/prompts.py, following the existing
    parenthesized-string style of START_TEXT / HELP_TEXT. Set its value to the exact string
    `"I only understand text messages — send me a question and I'll reply!"` (helpful-nudge
    tone per D-07). Place it alongside the other user-facing constants. Do NOT remove or
    modify TRUNCATION_NOTE (added in plan 05-01) or any existing constant.
  </action>
  <verify>
    <automated>python -c "from bot.prompts import NON_TEXT_REPLY; assert NON_TEXT_REPLY == \"I only understand text messages — send me a question and I'll reply!\", repr(NON_TEXT_REPLY)"</automated>
  </verify>
  <acceptance_criteria>
    - `bot/prompts.py` contains `NON_TEXT_REPLY` at module level
    - `from bot.prompts import NON_TEXT_REPLY` succeeds
    - `NON_TEXT_REPLY` equals exactly `"I only understand text messages — send me a question and I'll reply!"`
    - TRUNCATION_NOTE (from plan 05-01) still imports successfully
  </acceptance_criteria>
  <done>NON_TEXT_REPLY constant exists with the exact value; existing constants untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add handle_non_text handler, register it, and test it</name>
  <files>bot/handlers.py, bot/main.py, tests/test_handlers.py</files>
  <read_first>
    - bot/handlers.py (start/help_cmd minimal handler pattern at lines 11-20; the `if update.message is None: return` guard)
    - bot/main.py (imports at line 9; handler registrations at lines 27-29 — add the new handler AFTER the existing handle_text MessageHandler at line 29)
    - tests/test_handlers.py (asyncio.run() idiom; _make_update helper)
    - .planning/phases/05-ux-polish/05-PATTERNS.md (handle_non_text reference lines 91-98; main.py registration lines 112-122; test bodies lines 261-271)
    - .planning/phases/05-ux-polish/05-CONTEXT.md (D-06 single inverted-filter handler, D-07 tone, D-08 applies to all users)
  </read_first>
  <behavior>
    - handle_non_text(update, context) with a normal update calls update.message.reply_text exactly once with NON_TEXT_REPLY
    - handle_non_text(update, context) with update.message = None returns without raising and without calling reply_text (no-op guard)
    - handle_non_text performs NO allowed_chat_ids check — it replies for every user (D-08)
  </behavior>
  <action>
    In bot/handlers.py:
    1. Add `NON_TEXT_REPLY` to the `from .prompts import ...` line (it will already import
       HELP_TEXT, START_TEXT, and TRUNCATION_NOTE after plan 05-01).
    2. Add a new handler `async def handle_non_text(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:`
       following the exact minimal pattern of `start` / `help_cmd`: early-return guard
       `if update.message is None: return`, then `await update.message.reply_text(NON_TEXT_REPLY)`.
       Do NOT add any allowed_chat_ids / auth check — per D-08 the guard applies to all users,
       including unauthorized ones. Do NOT read update.message.text (media messages have no text).

    In bot/main.py:
    3. Add `handle_non_text` to the `from .handlers import handle_text, help_cmd, start` import
       (keep alphabetical-ish ordering consistent with the existing line).
    4. After the existing line `app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_text))`
       (line 29), add:
       `app.add_handler(MessageHandler(~filters.TEXT & ~filters.COMMAND, handle_non_text))`.
       The inverted filter `~filters.TEXT & ~filters.COMMAND` (D-06) catches photo/voice/
       sticker/video/document with no per-type branching. It must be registered AFTER the
       text handler so text still routes to handle_text.

    In tests/test_handlers.py:
    5. Add `handle_non_text` to the `from bot.handlers import ...` line; add `NON_TEXT_REPLY`
       to the `from bot.prompts import ...` line.
    6. Add these tests using the existing asyncio.run() idiom:
       - `test_handle_non_text_sends_guard_message`: build update via `_make_update()`, run
         `asyncio.run(handle_non_text(update, MagicMock()))`, assert
         `update.message.reply_text.assert_awaited_once_with(NON_TEXT_REPLY)`.
       - `test_handle_non_text_no_message_is_noop`: set `update.message = None`, run the
         handler, assert it does not raise (no further assertion needed beyond completing).
    Write the tests FIRST, confirm they fail (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /c/Users/boths/Desktop/telegram_bot_ai && python -m pytest tests/test_handlers.py -q && ruff check bot/ tests/ && ruff format --check bot/ tests/ && mypy bot/ --ignore-missing-imports</automated>
  </verify>
  <acceptance_criteria>
    - `bot/handlers.py` contains `async def handle_non_text(` with an `if update.message is None: return` guard and `reply_text(NON_TEXT_REPLY)`
    - `bot/handlers.py` handle_non_text contains NO reference to `allowed_chat_ids` (grep -v '^#' bot/handlers.py shows allowed_chat_ids only inside handle_text)
    - `bot/main.py` imports `handle_non_text` and registers `MessageHandler(~filters.TEXT & ~filters.COMMAND, handle_non_text)` AFTER the handle_text registration
    - `pytest tests/test_handlers.py -q` exits 0 with both new guard tests passing
    - `ruff check`, `ruff format --check`, and `mypy bot/ --ignore-missing-imports` all exit 0
  </acceptance_criteria>
  <done>
    handle_non_text replies with NON_TEXT_REPLY for all users (no auth check), is registered with the inverted filter after the text handler, no-ops on None messages, and all tests pass under ruff/mypy clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram user → bot handler | Untrusted non-text update (photo/voice/sticker/video/document) crosses into handle_non_text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | handle_non_text reading message contents | mitigate | Handler never reads media payload, file_id, or message.text — it only sends a fixed constant reply; no user content is logged or echoed |
| T-05-05 | Denial of Service | guard replies to unauthorized users (D-08) | accept | One fixed-string reply_text per non-text update; no LLM call is made (cheaper than the text path); public-bot cost risk is an accepted project-level decision with the OpenAI billing cap as the control |
| T-05-06 | Tampering | NON_TEXT_REPLY rendered to user | accept | Static constant sent as plain text with no parse_mode — no injection surface |
| T-05-SC | Tampering | dependency installs | mitigate | No new packages — uses already-installed python-telegram-bot filters and stdlib; no package-manager install tasks in this plan |
</threat_model>

<verification>
- `python -m pytest tests/test_handlers.py -q` → exit 0, includes the 2 new guard tests
- `ruff check bot/ tests/` → exit 0
- `ruff format --check bot/ tests/` → exit 0
- `mypy bot/ --ignore-missing-imports` → exit 0
- Manual grep: `grep -n "handle_non_text" bot/main.py` shows both the import and the MessageHandler registration with `~filters.TEXT & ~filters.COMMAND`
</verification>

<success_criteria>
- Non-text messages get NON_TEXT_REPLY instead of a silent drop (D-07)
- A single inverted-filter handler catches all non-text types, no per-type branching (D-06)
- The guard fires for every user including unauthorized ones (D-08)
- All tests pass; ruff + mypy clean
</success_criteria>

<output>
Create `.planning/phases/05-ux-polish/05-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 05-02)

- `bot/prompts.py` → `NON_TEXT_REPLY` (constant)
- `bot/handlers.py` → `async def handle_non_text(update, context) -> None` (new handler)
- `bot/handlers.py` → new import: `NON_TEXT_REPLY` (from .prompts)
- `bot/main.py` → import of `handle_non_text`
- `bot/main.py` → `app.add_handler(MessageHandler(~filters.TEXT & ~filters.COMMAND, handle_non_text))` registration
- `tests/test_handlers.py` → `test_handle_non_text_sends_guard_message`, `test_handle_non_text_no_message_is_noop`
</content>
